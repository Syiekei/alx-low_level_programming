#include "main.h"
#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0;
while (str[i])
{
_putchar(str[i]);
i++;
}
_putchar('\n');
}
puts("\"Programming is like building a multilingual puzzle");
{
return (0);
}

